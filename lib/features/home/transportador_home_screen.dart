import 'package:flutter/material.dart';

class TransportadorHomeScreen extends StatelessWidget {
  const TransportadorHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Transportador Home Screen'),
      ),
    );
  }
}
