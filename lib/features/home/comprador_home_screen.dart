import 'package:flutter/material.dart';

class CompradorHomeScreen extends StatelessWidget {
  const CompradorHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Comprador Home Screen'),
      ),
    );
  }
}
