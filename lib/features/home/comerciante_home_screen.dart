import 'package:flutter/material.dart';

class ComercianteHomeScreen extends StatelessWidget {
  const ComercianteHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Comerciante Home Screen'),
      ),
    );
  }
}
